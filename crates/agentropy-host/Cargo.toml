[package]
name = "agentropy-host"
version = "0.1.0"
edition = "2021"
rust-version = "1.83"

[dependencies]
anyhow = "1"
axum = "0.8"
host-api = { path = "../host-api" }
tokio = { version = "1.43", features = ["macros", "rt-multi-thread", "signal", "sync"] }
crossterm = "0.28"

[dev-dependencies]
tempfile = "3"
