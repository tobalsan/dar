[package]
name = "dar-system-files"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "System-file resolver: assembles an agent identity context from AGENTS.md and system_files"
rust-version = "1.83"

[lib]
name = "system_files"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
serde_yaml = "0.9"
